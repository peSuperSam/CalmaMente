// lib/styles/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFF1d7c8d);
  static const secondaryColor = Color(0xFF35A5AB);
  static const backgroundColor = Colors.white;
  static const cardBackground = Color(0xFFE1766B);
  static const textColor = Colors.white;
  static const shadowColor = Color(0xFF5A434B);

  // Cores adicionais para aprimorar a UI
  static const accentColor = Color(0xFFf27d14);
  static const primaryColorLight = Color(0xFF4ea5b1);
  static const primaryColorDark = Color(0xFF105e6d);

  // Adicione outras cores da paleta conforme necessário
}
